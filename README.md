# Chang Li AI Product Design Portfolio

React + Vite personal portfolio site for Chang Li / 李昶.

## Run

```bash
npm install
npm run dev
```

The site uses generated local assets in `public/assets` and links to copied case study pages in `public/cases`.
